name: iris
description: Object Detection Flutter App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.1
  http: ^1.1.0
  # tflite_flutter: ^0.10.4
  image: ^4.5.4  # Fixed version to avoid compatibility issues
  image_picker: ^1.0.4
  flutter_tts: ^4.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/model/